variables:
  latest_version: "2.0"

collections:
  source:
    output: true
    permalink: '/output/%{latest_version}/:dirpath'

defaults:
  -
    scope:
      path: ""
      type: "source"
    values:
      version: "%{latest_version}"
